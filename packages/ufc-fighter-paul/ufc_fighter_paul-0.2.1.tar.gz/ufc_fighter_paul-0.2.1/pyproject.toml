[project]
name = "ufc-fighter-paul"
version = "0.2.1"
description = "In commemoration of paul's last dance"
authors = [
    { name = "pppaal", email = "pjyrhee@gmail.com" },
]
dependencies = []
requires-python = ">=3.9"
readme = "README.md"

[project.license]
text = "MIT"

[build-system]
requires = [
    "pdm-backend",
]
build-backend = "pdm.backend"

[tool.pdm.dev-dependencies]
dev = [
    "pytest>=7.4.0",
]
