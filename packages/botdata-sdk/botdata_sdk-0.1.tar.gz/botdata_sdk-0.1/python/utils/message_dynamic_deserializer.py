def deserialize_message(proto_desc, message_content):
    """
    Given proto_desc in bytes and message in bytes, deserialize message in runtime

    Args:
        proto_desc (bytes): protobuf definition in bytes
        message_content (bytes): message in bytes

    Returns:
        message: class instance
    """
    message = None

    return message
