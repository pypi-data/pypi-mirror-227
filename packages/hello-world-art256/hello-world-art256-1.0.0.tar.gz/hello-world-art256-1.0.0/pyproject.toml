[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "hello-world-art256"
version = "1.0.0"
authors = [
    { name="Arthur Benjamin Angeles", email="abyangeles28@gmail.com" },
]
description = "Greets the user's name."
readme = "README.md"

[project.scripts]
hello-user = "hello_user.hello:main"