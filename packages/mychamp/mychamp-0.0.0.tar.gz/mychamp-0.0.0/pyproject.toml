[build-system]
    requires = ["setuptools>=61.0"]
    build-backend = "setuptools.build_meta"
[project]
    name = "mychamp"
    version = "0.0.0"
    dependencies = [
        "numpy"
    ]
    authors = [
        {name = "Jacob Striebel"}
    ]
    description = "MyChamp"
[project.scripts]
    python_project_stub = "mychamp.main:main"
[project.urls]
    "Homepage" = "https://github.com/striebel/mychamp"
