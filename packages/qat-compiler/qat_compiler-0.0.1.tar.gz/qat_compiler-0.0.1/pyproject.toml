[tool.poetry]
name = "QAT-Compiler"
version = "0.0.1"
description = ""
authors = ["Software OQC"]
readme = "README.rst"
packages = [
    { include = "qat", from = "src/QAT/" }
]

[tool.poetry.dependencies]
python = ">=3.8.1,<3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
