[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "reggia"
version = "0.1.0-alpha"
description = "A pythonic-style library that makes regular expressions (regex) simple."
readme = "README.md"
requires-python = ">=3.8"
license = {file = "LICENSE"}
keywords = ['python3', 'python', 'regex']
authors = [
	{ name="Jesse C. Sealand", email="jesse.sealand@outlook.com" }
	]
#maintainers
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
	]
dependencies = []

#dynamic
#entry-points
#gui-scripts

[project.optional-dependencies]
#gui = ["PyQt5"]

[project.urls]
"Homepage" = "https://github.com/jesse-sealand/reggia"
"Bug Tracker" = "https://github.com/jesse-sealand/reggia/issues"

[project.scripts]
#spam-cli = "spam:main_cli"

[project.gui-scripts]
#spam-gui = "spam:main_gui"

[project.entry-points."spam.magical"]
#tomatoes = "spam:main_tomatoes"