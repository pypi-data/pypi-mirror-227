[tool.poetry]
name = "ltr-datasets"
version = "0.1.2"
description = ""
authors = ["Philipp Hager <philipp.konstantin.hager@gmail.com>"]
readme = "README.md"
packages = [{include = "ltr_datasets"}]

[tool.poetry.dependencies]
python = "^3.11"
scikit-learn = "^1.3.0"
wget = "^3.2"
pandas = "^2.0.3"
pyarrow = "^13.0.0"
torch = "^2.0.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
