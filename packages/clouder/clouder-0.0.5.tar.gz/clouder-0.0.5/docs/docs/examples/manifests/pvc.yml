kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: nginx-logs
  namespace: nginx-example
  labels:
    app: nginx
spec:
  storageClassName: defauilt
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
