[tool.poetry]
name = "almanak"
version = "0.1.0"
description = "A placeholder for the incoming Almanak package"
authors = ["Lars <lars@suanet.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
