from setuptools import setup

setup(
    name='cdonut',
    version='0.1',
    description='A package to bring the c language donut meme to python, just import it to your code, then type: cdonut()',
    author='Omar',
    packages=['cdonut'],
    install_requires=[
        'numpy',
    ],
)