from setuptools import setup, find_packages

setup(
    name='max_min_python_heaps',
    version='0.3',
    packages=find_packages(),
    install_requires=[
        # List any dependencies here
    ],
    description='This is a plug in for python that provide min and max heap functionalities (without using the * -1 for max heap)',
    author='Oussama Ait Lamaalam',
    author_email='oussamaait2001@gmail.com',
    url='https://github.com/0u55ama/Heap-plug-in',
    keywords=['python', 'plugin', 'heap', 'heapq', 'max heap', 'min heap', 'priority queue'],
)
