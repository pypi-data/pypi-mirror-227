[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "ipfs-http-client-sdk"
version = "0.1.0"
description = "IPFS HTTP Client for Python"
authors = ["Niraj <niraj@polywrap.io>"]
readme = "README.rst"

[tool.poetry.dependencies]
python = "^3.10"
polywrap = "^0.1.0b7"

[tool.poetry.group.example.dependencies]
pillow = "^10.0.0"

