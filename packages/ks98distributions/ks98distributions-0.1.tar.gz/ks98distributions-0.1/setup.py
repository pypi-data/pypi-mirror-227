from setuptools import setup

setup(name='ks98distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['ks98distributions'],
      zip_safe=False)
